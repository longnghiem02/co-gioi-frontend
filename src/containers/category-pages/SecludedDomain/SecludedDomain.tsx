import React from 'react';
import Result from '../../components/Result';
import { CATEGORY_ITEM_TITLE, RESULT_TYPE } from '../../../common/utils';

function SecludedDomain() {
	return <Result title={CATEGORY_ITEM_TITLE.SECLUDED_DOMAIN} type={RESULT_TYPE.SECLUDED_DOMAIN} />;
}

export default SecludedDomain;
