import React from 'react';
import Result from '../../components/Result';
import { CATEGORY_ITEM_TITLE, RESULT_TYPE } from '../../../common/utils';

function GuFormation() {
	return <Result title={CATEGORY_ITEM_TITLE.GU_FORMATION} type={RESULT_TYPE.GU_FORMATION} />;
}

export default GuFormation;
