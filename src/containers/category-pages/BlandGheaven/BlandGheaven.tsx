import React from 'react';
import Result from '../../components/Result';
import { CATEGORY_ITEM_TITLE, RESULT_TYPE } from '../../../common/utils';

function BlandGheaven() {
	return <Result title={CATEGORY_ITEM_TITLE.BLAND_GHEAVEN} type={RESULT_TYPE.BLAND_GHEAVEN} />;
}

export default BlandGheaven;
